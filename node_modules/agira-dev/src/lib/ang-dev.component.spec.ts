import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AngDevComponent } from './ang-dev.component';

describe('AngDevComponent', () => {
  let component: AngDevComponent;
  let fixture: ComponentFixture<AngDevComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ AngDevComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AngDevComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
