import { TestBed } from '@angular/core/testing';

import { AngDevService } from './ang-dev.service';

describe('AngDevService', () => {
  let service: AngDevService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(AngDevService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
